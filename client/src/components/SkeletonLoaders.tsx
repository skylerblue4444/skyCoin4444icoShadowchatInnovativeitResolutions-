import React from 'react';\n\n/**\n * Skeleton Loader Components for smooth UI loading states\n */\n\ninterface SkeletonProps {\n  className?: string;\n  count?: number;\n}\n\n/**\n * Base Skeleton component\n */\nexport function Skeleton({ className = '', ...props }: React.HTMLProps<HTMLDivElement>) {\n  return (\n    <div\n      className={`animate-pulse bg-gradient-to-r from-zinc-900 via-zinc-800 to-zinc-900 rounded ${className}`}\n      {...props}\n    />\n  );\n}\n\n/**\n * Profile Card Skeleton\n */\nexport function ProfileCardSkeleton() {\n  return (\n    <div className=\"space-y-4 rounded-lg border border-zinc-800 bg-zinc-950 p-6\">\n      <div className=\"flex gap-4\">\n        <Skeleton className=\"h-16 w-16 rounded-full\" />\n        <div className=\"flex-1 space-y-2\">\n          <Skeleton className=\"h-4 w-3/4\" />\n          <Skeleton className=\"h-3 w-1/2\" />\n        </div>\n      </div>\n      <Skeleton className=\"h-20\" />\n      <div className=\"flex gap-2\">\n        <Skeleton className=\"h-10 flex-1\" />\n        <Skeleton className=\"h-10 flex-1\" />\n      </div>\n    </div>\n  );\n}\n\n/**\n * List Item Skeleton\n */\nexport function ListItemSkeleton({ count = 3 }: SkeletonProps) {\n  return (\n    <div className=\"space-y-3\">\n      {Array.from({ length: count }).map((_, i) => (\n        <div key={i} className=\"flex gap-4 rounded-lg border border-zinc-800 bg-zinc-950 p-4\">\n          <Skeleton className=\"h-12 w-12 rounded\" />\n          <div className=\"flex-1 space-y-2\">\n            <Skeleton className=\"h-4 w-3/4\" />\n            <Skeleton className=\"h-3 w-1/2\" />\n          </div>\n          <Skeleton className=\"h-8 w-16\" />\n        </div>\n      ))}\n    </div>\n  );\n}\n\n/**\n * Card Grid Skeleton\n */\nexport function CardGridSkeleton({ count = 6 }: SkeletonProps) {\n  return (\n    <div className=\"grid gap-4 md:grid-cols-2 lg:grid-cols-3\">\n      {Array.from({ length: count }).map((_, i) => (\n        <div key={i} className=\"space-y-3 rounded-lg border border-zinc-800 bg-zinc-950 p-4\">\n          <Skeleton className=\"h-40 w-full rounded\" />\n          <Skeleton className=\"h-4 w-3/4\" />\n          <Skeleton className=\"h-3 w-1/2\" />\n          <Skeleton className=\"h-10 w-full\" />\n        </div>\n      ))}\n    </div>\n  );\n}\n\n/**\n * Table Skeleton\n */\nexport function TableSkeleton({ count = 5 }: SkeletonProps) {\n  return (\n    <div className=\"space-y-2 rounded-lg border border-zinc-800 bg-zinc-950 p-4\">\n      {/* Header */}\n      <div className=\"flex gap-4 border-b border-zinc-800 pb-4\">\n        <Skeleton className=\"h-4 flex-1\" />\n        <Skeleton className=\"h-4 flex-1\" />\n        <Skeleton className=\"h-4 flex-1\" />\n        <Skeleton className=\"h-4 w-20\" />\n      </div>\n      {/* Rows */}\n      {Array.from({ length: count }).map((_, i) => (\n        <div key={i} className=\"flex gap-4 py-3\">\n          <Skeleton className=\"h-4 flex-1\" />\n          <Skeleton className=\"h-4 flex-1\" />\n          <Skeleton className=\"h-4 flex-1\" />\n          <Skeleton className=\"h-4 w-20\" />\n        </div>\n      ))}\n    </div>\n  );\n}\n\n/**\n * Dashboard Stats Skeleton\n */\nexport function DashboardStatsSkeleton({ count = 4 }: SkeletonProps) {\n  return (\n    <div className=\"grid gap-4 md:grid-cols-2 lg:grid-cols-4\">\n      {Array.from({ length: count }).map((_, i) => (\n        <div key={i} className=\"space-y-3 rounded-lg border border-zinc-800 bg-zinc-950 p-4\">\n          <Skeleton className=\"h-4 w-1/2\" />\n          <Skeleton className=\"h-8 w-3/4\" />\n          <Skeleton className=\"h-3 w-1/3\" />\n        </div>\n      ))}\n    </div>\n  );\n}\n\n/**\n * Chat Message Skeleton\n */\nexport function ChatMessageSkeleton({ count = 5 }: SkeletonProps) {\n  return (\n    <div className=\"space-y-4\">\n      {Array.from({ length: count }).map((_, i) => (\n        <div key={i} className={`flex ${i % 2 === 0 ? 'justify-start' : 'justify-end'}`}>\n          <div className={`space-y-2 ${i % 2 === 0 ? 'max-w-xs' : 'max-w-xs'}`}>\n            <Skeleton className={`h-10 ${i % 2 === 0 ? 'w-48' : 'w-56'} rounded-lg`} />\n            <Skeleton className=\"h-3 w-20\" />\n          </div>\n        </div>\n      ))}\n    </div>\n  );\n}\n\n/**\n * Feed Post Skeleton\n */\nexport function FeedPostSkeleton({ count = 3 }: SkeletonProps) {\n  return (\n    <div className=\"space-y-4\">\n      {Array.from({ length: count }).map((_, i) => (\n        <div key={i} className=\"space-y-4 rounded-lg border border-zinc-800 bg-zinc-950 p-4\">\n          {/* Header */}\n          <div className=\"flex gap-3\">\n            <Skeleton className=\"h-10 w-10 rounded-full\" />\n            <div className=\"flex-1 space-y-2\">\n              <Skeleton className=\"h-4 w-1/2\" />\n              <Skeleton className=\"h-3 w-1/4\" />\n            </div>\n          </div>\n          {/* Content */}\n          <Skeleton className=\"h-32 w-full rounded\" />\n          {/* Actions */}\n          <div className=\"flex gap-4\">\n            <Skeleton className=\"h-8 w-16\" />\n            <Skeleton className=\"h-8 w-16\" />\n            <Skeleton className=\"h-8 w-16\" />\n          </div>\n        </div>\n      ))}\n    </div>\n  );\n}\n\n/**\n * Search Results Skeleton\n */\nexport function SearchResultsSkeleton({ count = 4 }: SkeletonProps) {\n  return (\n    <div className=\"space-y-3\">\n      {Array.from({ length: count }).map((_, i) => (\n        <div key={i} className=\"flex gap-4 rounded-lg border border-zinc-800 bg-zinc-950 p-4\">\n          <Skeleton className=\"h-24 w-24 rounded\" />\n          <div className=\"flex-1 space-y-2\">\n            <Skeleton className=\"h-4 w-3/4\" />\n            <Skeleton className=\"h-3 w-full\" />\n            <Skeleton className=\"h-3 w-2/3\" />\n            <div className=\"flex gap-2 pt-2\">\n              <Skeleton className=\"h-3 w-12\" />\n              <Skeleton className=\"h-3 w-12\" />\n              <Skeleton className=\"h-3 w-12\" />\n            </div>\n          </div>\n        </div>\n      ))}\n    </div>\n  );\n}\n\n/**\n * Analytics Chart Skeleton\n */\nexport function AnalyticsChartSkeleton() {\n  return (\n    <div className=\"space-y-4 rounded-lg border border-zinc-800 bg-zinc-950 p-6\">\n      <Skeleton className=\"h-6 w-1/3\" />\n      <div className=\"space-y-2\">\n        {Array.from({ length: 6 }).map((_, i) => (\n          <div key={i} className=\"flex gap-2\">\n            <Skeleton className=\"h-8 w-8\" />\n            <Skeleton className={`h-8 flex-1`} style={{ width: `${Math.random() * 100}%` }} />\n          </div>\n        ))}\n      </div>\n    </div>\n  );\n}\n\n/**\n * User Profile Skeleton\n */\nexport function UserProfileSkeleton() {\n  return (\n    <div className=\"space-y-6\">\n      {/* Cover */}\n      <Skeleton className=\"h-40 w-full rounded-lg\" />\n      {/* Profile Info */}\n      <div className=\"space-y-4 px-4\">\n        <div className=\"flex gap-4\">\n          <Skeleton className=\"h-24 w-24 rounded-full\" />\n          <div className=\"flex-1 space-y-2\">\n            <Skeleton className=\"h-6 w-1/2\" />\n            <Skeleton className=\"h-4 w-2/3\" />\n            <Skeleton className=\"h-4 w-1/2\" />\n          </div>\n        </div>\n        {/* Stats */}\n        <div className=\"grid grid-cols-3 gap-4\">\n          {Array.from({ length: 3 }).map((_, i) => (\n            <div key={i} className=\"space-y-2 text-center\">\n              <Skeleton className=\"h-6 w-full\" />\n              <Skeleton className=\"h-3 w-full\" />\n            </div>\n          ))}\n        </div>\n        {/* Bio */}\n        <Skeleton className=\"h-20 w-full\" />\n      </div>\n    </div>\n  );\n}\n\n/**\n * Marketplace Listing Skeleton\n */\nexport function MarketplaceListingSkeleton({ count = 8 }: SkeletonProps) {\n  return (\n    <div className=\"grid gap-4 md:grid-cols-2 lg:grid-cols-4\">\n      {Array.from({ length: count }).map((_, i) => (\n        <div key={i} className=\"space-y-3 rounded-lg border border-zinc-800 bg-zinc-950 p-3\">\n          <Skeleton className=\"h-40 w-full rounded\" />\n          <Skeleton className=\"h-4 w-3/4\" />\n          <Skeleton className=\"h-3 w-1/2\" />\n          <div className=\"flex justify-between\">\n            <Skeleton className=\"h-4 w-16\" />\n            <Skeleton className=\"h-4 w-12\" />\n          </div>\n          <Skeleton className=\"h-8 w-full\" />\n        </div>\n      ))}\n    </div>\n  );\n}\n\n/**\n * Loading Spinner\n */\nexport function LoadingSpinner() {\n  return (\n    <div className=\"flex items-center justify-center\">\n      <div className=\"h-12 w-12 animate-spin rounded-full border-4 border-zinc-700 border-t-blue-500\" />\n    </div>\n  );\n}\n\n/**\n * Loading Overlay\n */\nexport function LoadingOverlay() {\n  return (\n    <div className=\"fixed inset-0 flex items-center justify-center bg-black/50 backdrop-blur-sm\">\n      <div className=\"space-y-4 text-center\">\n        <LoadingSpinner />\n        <p className=\"text-sm text-zinc-400\">Loading...</p>\n      </div>\n    </div>\n  );\n}\n\n/**\n * Skeleton Wrapper Component\n */\ninterface SkeletonWrapperProps {\n  isLoading: boolean;\n  skeleton: React.ReactNode;\n  children: React.ReactNode;\n}\n\nexport function SkeletonWrapper({\n  isLoading,\n  skeleton,\n  children,\n}: SkeletonWrapperProps) {\n  if (isLoading) {\n    return <>{skeleton}</>;\n  }\n  return <>{children}</>;\n}\n\n/**\n * Pulse Animation Utility\n */\nexport function usePulseAnimation(duration: number = 2000) {\n  return `animate-pulse`;\n}\n
